{
  "name": "aq-react-native-miniapp-sdk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && npm run copy",
    "copy": "cp -R ./ios ../dist/ios/ && cp -R ./android ../dist/android/ && cp ./index.js ../dist",
    "clean": "rm -rf ../dist/ios && rm -rf ../dist/android && rm -rf ../dist/index.js"
  },
  "keywords": [
    "react-native"
  ],
  "author": "Ryan C. Brozo",
  "license": "UNLICENSED",
  "files": [
    "index.js",
    "android",
    "ios"
  ],
  "peerDependencies": {
    "react-native": "^0.48.x",
    "react": "16.0.0-alpha.12"
  }
}
